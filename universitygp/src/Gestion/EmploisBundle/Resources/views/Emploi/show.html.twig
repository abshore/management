{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Emploi</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Jour</th>
                <td>{{ entity.jour|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Heuredeb</th>
                <td>{{ entity.heureDeb }}</td>
            </tr>
            <tr>
                <th>Heurefin</th>
                <td>{{ entity.heureFin }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('emploi') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('emploi_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('emploi_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}

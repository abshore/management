

{% block body -%}
    <h1>Langue creation</h1>

    <form action="{{ path('langue_create', { 'idcursus': idcursus }) }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    
</ul>
{% endblock %}

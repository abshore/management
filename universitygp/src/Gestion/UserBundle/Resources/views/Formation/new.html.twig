

{% block body -%}
    <h1>Formation creation</h1>

    <form action="{{ path('formation_create', { 'idcursus': idcursus }) }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    
</ul>
{% endblock %}


    <form action="{{ path('cursus_createuser', { 'iduser': iduser }) }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

    


